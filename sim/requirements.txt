# COSMOS sim/ 파이썬 의존성 — 버전 고정본 (2026-09-18 해석·검증)
#
# 왜 고정하나: 버전을 안 박아두면 부원마다 다른 gymnasium/SB3가 깔린다. 그러면 학습 결과가
# 서로 재현되지 않고, 문제가 생겼을 때 "코드가 틀렸나, 환경이 다른가"를 구분할 수 없다.
# RL은 원래 결과가 튀는 분야라 환경 변수를 하나라도 줄여놔야 한다.
#
# 설치 (sim/ 폴더에서):
#   python -m venv venv
#   venv\Scripts\activate          # Mac/Linux: source venv/bin/activate
#   pip install -r requirements.txt
# 검증:
#   cd sim_stage1 && python sanity_check.py
#
# 버전을 올릴 땐: sanity_check.py + train.py(짧게)를 먼저 돌려보고, PR 설명에 "왜 올렸는지"를 남긴다.

gymnasium==1.3.0
numpy==2.5.3
PyYAML==6.0.3
stable_baselines3==2.9.0

# torch는 stable_baselines3가 알아서 끌고 온다. 일부러 고정하지 않는다 — OS·CPU/GPU마다
# 맞는 휠이 달라서, 버전을 박으면 오히려 설치가 깨지는 쪽이 많다.
# (참고: 2026-09-18 기준 해석 결과는 torch 2.14.0)
#
# tensorboard로 학습 곡선을 보고 싶으면 추가로: pip install "stable_baselines3[extra]"
