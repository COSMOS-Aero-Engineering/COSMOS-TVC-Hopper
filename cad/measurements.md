# 실측값 기록

캘리퍼스로 잰 부품 실측값을 여기 적고, `cad/hopper_params.scad`의 `MEASURED` 블록에 반영한 뒤 커밋한다.
값을 바꿀 때마다 이 표도 같이 업데이트할 것 — 나중에 "이 숫자가 어디서 나왔는지" 확인할 유일한 근거다.

| 부품 / 위치 | 측정값 (mm) | 측정 방법 | 측정자 | 날짜 | 비고 |
|---|---|---|---|---|---|
| EDF 외경(앞, 프로펠러쪽) | 71.9 (둘레 226mm 환산) | 줄자로 둘레 측정 후 ÷π | 민찬 | 2026-09-18 | 1차(9/13) 측정 72mm와 오차 0.5mm 이내로 일치 — 재확인 완료. `edf_housing_od`는 72 유지 |
| EDF 외경(뒤, 모터쪽) | 69.9 (둘레 219.5mm 환산) | 줄자로 둘레 측정 후 ÷π | 민찬 | 2026-09-18 | 1차 측정 70mm와 일치. `edf_body_od_min`는 70 유지 |
| EDF 마운트 홀 간격 | N/A | — | — | 2026-09-13 | 볼트 플랜지 없는 제품으로 확인됨(사진 확인) → 클램프 마운트로 설계 전환. 이 항목은 측정 대상 아님 |
| Teensy 보드 가로×세로 | ~~35.5 × 10~~ → **35 × 17 (최종, 2026-09-19)** | 자, 2회 재측정 | 민찬 | 2026-09-19 | 1차(9/18) "세로 10mm"는 측정 오류로 확인 — 재측정 결과 공식 스펙(36×18mm)과 거의 일치. `teensy_w`는 미리보기 전용 치수라 프린트 파츠 형상엔 영향 없음(핀헤더 열 간격 17.78mm는 규격 고정값) |
| 베인(vane) 서보 이어 홀 피치 | **33 (실측 확정)** | 캘리퍼스, SG90 실물(MG90S 대용 — 케이스 규격 동일) | 민찬 | 2026-09-19 | 조사자료 추정값(27.5~28.0)보다 5mm 큼 — `servo_ear_pitch`를 33으로 교체. 마운트판 폭은 이 값에 자동 연동되는 파라메트릭 구조라 CAD 재계산됨 |
| 베인 서보 몸통 긴변(접선방향) | **23 (실측 확정)** | 캘리퍼스, SG90 실물 | 민찬 | 2026-09-19 | 조사자료(22.5~23.0) 범위 안 — `servo_body_l`을 23으로 교체 |
| 베인 서보 몸통 길이(반경방향, 축 보스 제외) | **28 (실측 확정)** | 캘리퍼스, SG90 실물 | 민찬 | 2026-09-19 | 조사자료 추정값(22.5)보다 5.5mm 큼 — `servo_body_h`를 28로 교체. 프린트 형상엔 안 쓰이는 클리어런스 참고값(미리보기·echo 전용) |
| 베인 서보 몸통 폭(두께) | 미측정 — 12.2 유지 | — | — | — | ⚠️ 아직 안 잼. `servo_body_w` 플레이스홀더 유지 |
| 베인 서보 축→가까운 이어홀 거리 | 미측정 — 9.9 유지 | — | — | — | ⚠️ 아직 안 잼. `servo_ear_to_shaft` 플레이스홀더 유지. "축은 0.3(cm)"이라 준 값은 출력축 지름으로 판단, 이 항목 아님 |
| 배터리 가로×세로×두께 | ~~34 × 74~~ → **63.5 × 32.5 × 15 (최종, 2026-09-19)** | 자, 3축 전부 재측정 | 민찬 | 2026-09-19 | 1차(9/18)는 2축만 쟀고 오차도 있었음 — 재측정으로 3축 전부 확정, `batt_l/w/h` 교체 |

**상판 두께는 측정 항목 아님** — `cad/print_parts/top_plate.stl`로 3D 프린트할 파츠라 잴 실물이 없다. 마스터 설계 §4에 이미 `top_plate_t = 4mm`로 잡혀 있으니, 그대로 쓸지 강성 보고 바꿀지만 결정해서 `hopper_params.scad`에 반영. (플랜B로 합판 상판을 쓰게 되면 그때만 실측 필요.)

측정할 때 참고:
- 캘리퍼스 0점(zeroing) 먼저 확인.
- 같은 곳을 2번 재서 값이 다르면 셋째 값까지 재고 중간값 사용.
- 표에 없는 항목이 필요하면 행을 추가하고 커밋 메시지에 `[cad]` 태그 붙이기.
